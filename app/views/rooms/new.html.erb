<%# 直接アクセス・バリデーションエラー時のフォールバックページ（通常はモーダルから作成） %>
<div class="card bg-base-100 shadow-sm max-w-md mx-auto">
  <div class="card-body">
    <h2 class="card-title mb-2">部屋の新規作成</h2>

    <%= render "form", room: @room %>

    <%= link_to "一覧へ戻る", rooms_path, class: "link link-hover text-sm mt-2" %>
  </div>
</div>
